import { PatternBackgroundDirective } from './pattern-background.directive';

describe('PatternBackgroundDirective', () => {
  it('should create an instance', () => {
    // const directive = new PatternBackgroundDirective();
    //expect(directive).toBeTruthy();
  });
});
