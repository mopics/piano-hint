// if angular 4 needs to know a values of a sass varibable put them in here as an static field

export class SassVars {
    static patternTop:number = 24;
    static patternMenuTop:number = 17;
}

// TODO: find a nice npm package or write one our selves that extracts sass variables and puts them in a TypeScript class like this one.